{
  "name": "trackhs-mcp-remote",
  "version": "1.0.0",
  "type": "module",
  "description": "MCP Remote Server para integración con Track HS API",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && wrangler deploy",
    "dev": "wrangler dev",
    "test": "wrangler dev --local",
    "clean": "rm -rf dist",
    "start": "node dist/index.js"
  },
  "keywords": [
    "mcp",
    "trackhs",
    "api",
    "remote",
    "cloudflare"
  ],
  "author": "Track HS Team",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}